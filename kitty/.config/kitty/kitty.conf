# Kitty configuration
# Migrated from WezTerm

# Terminal type - required for kitty graphics protocol
term xterm-kitty

# Font configuration
font_family GeistMono Nerd Font
font_size 13
modify_font cell_height 125%

# Window appearance
hide_window_decorations yes
window_padding_width 30

# Scrollback
scrollback_lines 10000

# Cursor
cursor_beam_thickness 2
cursor_shape beam

# Mouse
mouse_hide_wait 3.0

# Shell
shell fish

# Disable confirm on close
confirm_os_window_close 0

# Tab bar (disabled like WezTerm)
tab_bar_style hidden

# kitty-scrollback.nvim
action_alias kitty_scrollback_nvim kitten /home/daphen/.local/share/nvim/lazy/kitty-scrollback.nvim/python/kitty_scrollback_nvim.py

# Keybindings
# Ctrl+S for scrollback/copy mode in nvim
map ctrl+s kitty_scrollback_nvim

# Ctrl+Shift+V paste
map ctrl+shift+v paste_from_clipboard

# Ctrl+Shift+R reload config
map ctrl+shift+r load_config_file

# Allow remote control (for theme switching)
allow_remote_control yes
listen_on unix:/tmp/kitty-{kitty_pid}

# Include theme (will be generated by theme-manager)
include theme.conf
