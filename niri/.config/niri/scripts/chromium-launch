#!/usr/bin/env bash
# Launch Helium personal profile (Default). Always uses AMD iGPU — PRIME offload
# adds too much overhead for browsers. Games use NVIDIA via programs.steam.
exec /etc/profiles/per-user/daphen/bin/helium \
    --profile-directory=Default --class=helium-personal "$@"
