/**
 * Simple test plugin to verify OpenCode plugin loading
 * DISABLED - to avoid console pollution
 */

export default async (context) => {
  // Plugin disabled to avoid console pollution
  return {};
};